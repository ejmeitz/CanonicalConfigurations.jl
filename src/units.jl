# When energy is for exmaple, kcal/mol, the freuency squared has units kcal / mol / Å^2
# @derived_dimension MolarFrequency (𝐍^-1/2) * inv(𝐓) true

function check_units(kB, hbar, temp, mass, freqs)

end